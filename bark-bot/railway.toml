[build]
builder = "NIXPACKS"

[deploy]
preDeployCommand = "uv run python scripts/init_db.py"
startCommand = "uv run uvicorn main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
